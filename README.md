# Bay Area Pierogi — public website

Static coming-soon page for https://bayareapierogi.com (ZK Bay Foods LLC).

Deploy: push to `main`; GitHub Pages serves the repo root. DNS lives in Cloudflare (DNS-only records pointing at GitHub Pages).
